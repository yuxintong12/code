#include<iostream>

using namespace std;
const int N = 100010;
int tree[N][26],cnt[N];
char put[N];
int main()
{
    int n;
    cin >> n;
    while(n--)
    {
        char op[2];
        scanf("%s%s",op,put);
        if(op[1] == )
    }
}